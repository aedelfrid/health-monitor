# Project 1: The "Real-Time Health Monitor" (Go + WebSockets + React)

This shows you understand Go’s concurrency and JS’s real-time capabilities.

    The Backend (Go): A small service that monitors system stats (CPU/RAM usage) or pings a list of URLs and broadcasts the status via WebSockets.

    The Frontend (Next.js): A clean dashboard with "Status Lights" and a live-updating line chart (using Chart.js or Recharts).

    The "Hire Me" Factor: Shows you can handle streaming data and concurrent processes in Go.

    -----------------

## Backend

## Frontend